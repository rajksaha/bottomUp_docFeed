<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bottomUp.myBatis.persistence.PrescriptionComplainMapper">

	<insert id="create" parameterType="prescriptionComplain" useGeneratedKeys="true" keyProperty="complainID" >

		INSERT INTO `prescription_complain`(
			`appointMentID`,
			`symptomID`,
			`durationNum`,
			`durationType`,
			`detail`,
			`updatedBy`,
			`updatedOn`,
			`createdBy`,
			`createdOn`
		)
		VALUES(
			#{appointMentID},
			#{symptomID},
			#{durationNum},
			#{durationType},
			#{detail},
			#{updatedBy},
			#{updatedOn},
			#{createdBy},
			#{createdOn}
		);


	</insert>
	
	<update id="update" parameterType="prescriptionComplain">
		UPDATE `prescription_complain`
		SET
		`appointMentID` = #{appointMentID},
		`symptomID` = #{symptomID},
		`durationNum` = #{durationNum},
		`durationType` =#{durationType},
		`detail` = #{detail},
		`updatedBy` = #{doctorID},
		`updatedOn` = #{doctorID}
		WHERE complainID = #{complainID}
	</update>

	<select id="getByID" parameterType="long" resultType="prescriptionComplain">
		SELECT
			`complainID`,
			`appointMentID`,
			`symptomID`,
			`durationNum`,
			`durationType`,
			`detail`,
			`updatedBy`,
			`updatedOn`,
			`createdBy`,
			`createdOn`
		FROM `prescription_complain`

		WHERE complainID = #{complainID}
	</select>

	<select id="getByParam" parameterType="map" resultType="prescriptionComplain">
		SELECT
		`complainID`,
		`appointMentID`,
		`symptomID`,
		`durationNum`,
		`durationType`,
		`detail`,
		`updatedBy`,
		`updatedOn`,
		`createdBy`,
		`createdOn`
		FROM `prescription_complain`
		WHERE 1 = 1
		<if test="complainID != null">
			AND complainID = #{complainID}
		</if>
		<if test="offset != null and limit != null and sortColumn != null and sortOrder != null">
			ORDER BY ${sortColumn} ${sortOrder}
			LIMIT #{limit} OFFSET #{offset}
		</if>
	</select>

    <delete id="delete" parameterType="map">
		DELETE  FROM prescription_complain
		WHERE 1=1
		<if test="prescription_complain != null">
			AND prescription_complain = #{prescription_complain}
		</if>
	</delete>


</mapper>